apiVersion: v1
kind: Service
metadata:
  name: {{ include "marcstreeterdev-frontend.fullname" . }}
  labels:
    {{- include "marcstreeterdev-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marcstreeterdev-frontend.selectorLabels" . | nindent 4 }} 